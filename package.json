{
  "name": "syncthing-observer",
  "version": "2.2.0",
  "private": true,
  "description": "Observes files received by a Syncthing hub and sends Pushover notifications",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/index.js",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/logger.js && node --check src/storage.js && node --check src/syncthing.js && node --check src/notifications.js && node --check src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  }
}
